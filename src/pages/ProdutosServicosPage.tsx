import { PlaceholderPage } from "@/components/PlaceholderPage"

export function ProdutosServicosPage() {
  return (
    <PlaceholderPage
      title="Produtos & Serviços"
      breadcrumb="Produtos & Serviços"
    />
  )
}
